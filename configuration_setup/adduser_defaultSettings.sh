#!/bin/bash

cat /etc/adduser.conf

# here some stuff is defined and applied when a new user is added into the system: QUOTA, home directory, UID starts from 1000 ... 

# Some config part, is also here:

/etc/login.defs
