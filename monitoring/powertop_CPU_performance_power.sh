#!/bin/bash

sudo apt-get install powertop

# to check CPU power performance
sudo powertop

# to generate an html report, accessible via browser:
sudo powertop --html

# Optimization available from bash ... use tab key to move to Tunables, move with the arrows and click Enter for changing to Good
